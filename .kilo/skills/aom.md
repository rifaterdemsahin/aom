# Adaptive Object Model skill

Load when changing domain-model docs or the explainer.

## Kernel
- Type Square: EntityType, Entity, PropertyType, Property
- Behavior: Strategy / Rule Objects
- Code is an interpreter of user metadata (Yoder & Johnson)

## Modern stack
- LLMs draft metadata; humans own invariants
- rclone moves YAML/Markdown between backends
- Projections: files, SQL, graph, vectors
- Blobs: Azure project storage via `dp-kv-deliverypilot`
- Photos: `5_Symbols/assets/images/`
