# ❓ Open Questions

> **Stage 1: Real Unknown**

---

1. When should a backend interpreter land on Cloudflare Workers vs stay static-only?
2. Which Azure Storage account is the project-scoped blob store for AOM metadata files?
3. Should Neo4j / Qdrant credentials for a future runtime live as names in `dp-kv-deliverypilot` now, or only when the interpreter exists?
4. Is Markdown+YAML the source of truth for EntityTypes, or a small JSON schema in Azure blobs?
